#!/bin/bash
docker exec {{ database_container_name }} psql -U {{ database_user }} postgres -tc "SELECT 1 FROM pg_database WHERE datname = '{{ database_name }}'" | grep -q 1 || docker exec {{ database_container_name }} psql -U {{ database_user }} postgres -c "CREATE DATABASE {{ database_name }}"
